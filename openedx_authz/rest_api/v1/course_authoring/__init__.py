"""REST API views exposing Course Authoring data.

This package holds the one documented exception to openedx-authz's domain
boundary. Decision item 3 in ADR 0026 places it in a subpackage named after the
domain that owns this data. See ADR 0015
for why the exception exists and ADR 0026 for the domain-ownership determination.
"""
